# Pacotes de sistema (instalar antes, via apt):
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0 python3-full
#
# Criar o venv herdando o GTK/PyGObject do sistema:
#   python3 -m venv --system-site-packages .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Obs: PyGObject e pycairo vem do apt (binding de sistema), por isso
# nao estao listados aqui — instala-los via pip falha sem as libs -dev.
numpy
matplotlib
